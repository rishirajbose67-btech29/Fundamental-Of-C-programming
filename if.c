#include<stdio.h>
int main()
{  int x,y;
printf("enter the value of two number:");
 scanf("%d, %d", &x, &y);
if(x>y)
{
    printf("x is greater value");
}
  else if(y>x)
{
     printf("y is the greater value");
}
   return 0;


}
